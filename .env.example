# MongoDB Connection String (optional — if unset, runs seamlessly with built-in in-memory database)
MONGODB_URI=

# JWT Secret for Session Signing
JWT_SECRET=mutual-fund-secure-session-secret-key-2026

# Gemini API Key (Server-side)
GEMINI_API_KEY=
